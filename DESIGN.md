# DESIGN.md — ChemAI 前端设计工程规格

> 生成依据：[36-设计系统](./36-设计系统.md) + [40-前端页面设计规格](./40-前端页面设计规格.md)
> 本文档是 AI 编码助手生成 ChemAI 前端代码的**唯一设计事实来源**。所有视觉决策、组件选型、交互行为均需以本文档为准。

---

## 第一段：设计参数结构化数据块

> 以下所有数值提取自 36 号设计系统，是 ChemAI 视觉层的完整 Token 定义。AI 生成任何 HTML/CSS 时，必须使用此处定义的 Token 名称（通过 Tailwind CSS 类名或内联 CSS 变量引用），禁止使用任意自由色值或间距。

### 1.1 色彩 Token

```yaml
colors:
  # === 主色层（不可混用用途） ===
  primary:
    oxford_blue:    "#002147"   # 系统的声音：主按钮、侧边栏、标题强调
    oxford_hover:   "#001a38"   # 牛津蓝悬停
    teal:           "#0d7377"   # AI 的声音：对话气泡、辅助按钮、化学元素强调
    teal_hover:     "#0a5c5f"   # 学术青悬停
    warm_paper:     "#faf8f5"   # 纸的声音：页面主背景、卡片底色

  # === 语义色：状态传达（浅色背景 + 深色文字标签式） ===
  semantic:
    text_body:      "#1a1a2e"   # 所有正文文字
    text_secondary: "#6b7280"   # 辅助说明、时间戳、标签
    bg_warm:        "#f5f0e8"   # 侧边栏、暖色调区域
    accent_warm:    "#d4a574"   # 卡片装饰条、分割线
    deep_emphasis:  "#312e81"   # 极高视觉权重的强调元素
    link_blue:      "#3b82f6"   # 超链接、次要强调

  # === 状态色 ===
  status:
    pass_bg:        "极浅绿"     # 审核通过背景
    pass_text:      "#2c6e49"   # 通过绿：审核通过、正确标记、成功提示
    warn_bg:        "极浅黄"     # 警告背景
    warn_text:      "深棕"       # 审核警告
    block_bg:       "极浅红"     # 阻断背景
    block_text:     "#b43c28"   # 错误红：审核阻断、错误提示、删除操作

  # === 装饰色：氢原子光谱四色（仅背景渐变/装饰分割线/加载动画） ===
  decorative:
    h_alpha:        "Hα 红"     # 656nm 波段
    h_beta:         "Hβ 青绿"   # 486nm 波段
    h_gamma:        "Hγ 紫"     # 434nm 波段
    h_delta:        "Hδ 靛蓝"   # 410nm 波段

  # === 对话气泡专用色 ===
  bubble:
    ai_bg:          "#e0f2f1"   # AI 气泡浅青背景 (Teal 的 10% 透明度版本)
    user_bg:        "#ffffff"   # 用户气泡白底
    user_border:    "浅灰"       # 用户气泡边框
```

### 1.2 字体 Token

```yaml
typography:
  heading:
    family:    "衬线展示字体"        # 学术书籍传统质感，品牌感
    example:   "Cormorant Garamond 或类似"
  body:
    family:    "IBM Plex Sans, Noto Sans SC, sans-serif"
    usage:     "正文、按钮、标签、表单"
  mono:
    family:    "JetBrains Mono, monospace"
    usage:     "化学式、代码、数据表格数值"
    critical:  "化学式上下标对齐依赖此等宽字体，不可替换为其他等宽字体"
```

### 1.3 间距与圆角 Token（Tailwind 预设）

```yaml
spacing:
  radius:
    button:       "8px"          # 按钮、输入框
    card:         "8px"          # 卡片（同按钮保持一致）
    bubble:       "12px"         # 对话气泡（比卡片更圆润，亲和感）
    modal:        "12px"         # 弹窗
  grid:
    base_unit:    "4px"          # 所有间距基于 4px 步进（Tailwind 默认）
    sidebar_w:    "240px"        # 桌面端侧边栏宽度
    drawer_w:     "480px"        # 右侧详情抽屉宽度
    mobile_w:     "430px"        # 学生端/家长端移动视口
    mobile_drawer:"280px"        # 学生端侧边栏抽屉宽度
    tabbar_h:     "56px"         # 顶部标题栏/底部 TabBar 高度
    input_area_h: "70px"         # 移动端底部输入区高度
  breakpoints:
    desktop:      "≥1024px"
    tablet:       "768-1023px"
    mobile:       "<768px"
```

### 1.4 化学式渲染 Token

```yaml
formula_rendering:
  engine:        "KaTeX + mhchem 扩展"
  syntax:
    molecule:    "\ce{H2O}"           → H₂O
    ion:         "\ce{SO4^2-}"        → SO₄²⁻
    half_rxn:    "\ce{Fe^3+ + e- -> Fe^2+}" → Fe³⁺ + e⁻ → Fe²⁺
    full_rxn:    "\ce{CH4 + 2O2 -> CO2 + 2H2O}" → CH₄ + 2O₂ → CO₂ + 2H₂O
  backend:       "API JSON 中纯文本化学式自动转换为上下标格式后返回前端"
```

### 1.5 技术栈约束

```yaml
tech_stack:
  html:          "Vanilla HTML（多页面，非 SPA）"
  css:           "Tailwind CSS CDN（原子类名，零编译）"
  js:            "Vanilla JS 模块化"
  exceptions:
    - "考试工作台页面：Vue 3 CDN（仅此页面，处理多 Tab 响应式切换）"
    - "化学式渲染：KaTeX CDN + mhchem 扩展"
  forbidden:
    - "禁止引入任何需要构建步骤的前端框架（React/Angular/Next.js）"
    - "禁止引入 PostCSS/Tailwind CLI 编译流程"
    - "禁止自定义 CSS 文件覆盖 Tailwind 预设色板"
```

---

## 第二段：可复用组件模式

> 以下组件通过对 40 号文档全部 14 个页面进行交叉扫描后抽象得出。每个组件标注了**出现页面**（出现频次 ≥2 的组件被认为是全局组件，应在所有页面间共享实现）。

### 2.1 全局布局组件（出现在 3+ 页面）

#### SIDEBAR — 侧边导航栏

```
出现页面: 1.1(教师Agent对话), 横向覆盖全部 7 个教师端页面
```

| 属性 | 规格 |
|------|------|
| 宽度 | 桌面 240px 固定，平板折叠为 48px（仅图标） |
| 背景 | Warm Paper `#f5f0e8` |
| 结构 | 品牌标识 → 主要操作按钮 → 导航菜单列表 → 底部快捷图标 |
| 导航项 | 元素符号图标（H/O/C/Fe）+ 文字标签，衬线字体加粗 |
| 状态 | 默认展开 / 折叠（仅图标）/ 当前页高亮（Oxford Blue 左侧 3px 指示条） |

#### TABBAR — 底部 Tab 导航（学生端）

```
出现页面: 2.1(AI助教), 2.2(练习), 2.3(错题), 2.5(个人报告) — 学生端全局
```

| 属性 | 规格 |
|------|------|
| 高度 | 56px |
| 背景 | 白色，顶部 1px 浅灰分割线 |
| Tab 项 | 4 项：AI 助教 / 练习 / 错题 / 我的 |
| 激活态 | Oxford Blue 填充（图标 + 文字） |
| 非激活态 | `#6b7280` 灰色（图标 + 文字） |

#### TABNAV — Tab 导航栏（教师端内容页）

```
出现页面: 1.2(考试工作台4个Tab), 1.4(学生管理筛选), 2.2(练习待完成/已完成), 2.3(错题筛选), 3.2(家长面板3个Tab)
```

| 属性 | 规格 |
|------|------|
| 实现方式 | 水平按钮组 + 下划线指示器（当前激活 Tab） |
| 激活态 | Oxford Blue 文字 + 底部 3px 蓝色下划线 |
| 非激活态 | `#6b7280` 灰色文字 |
| 交互 | 点击切换显示/隐藏对应内容区（非 SPA 路由） |

#### STATBAR — 页面顶部统计栏

```
出现页面: 1.3(4 KPI卡片), 1.4(4项统计), 2.3(3项统计), 2.4(3项统计), 3.2(4项统计)
```

| 属性 | 规格 |
|------|------|
| 布局 | 水平等宽网格（2-5 列，视内容量） |
| 每项结构 | 标签文字（`#6b7280` 小号） + 数字（Oxford Blue 大号） |
| 加载态 | 数字区域闪烁占位（骨架屏脉冲动画） |
| 空态 | 无数据时数字显示 `--` 而非 `0` |

---

### 2.2 对话系统组件（出现在 2+ 页面）

#### CHATBUBBLE — 对话气泡

```
出现页面: 1.1(教师Agent对话), 2.1(学生AI助教), 3.2(家长浮动AI助手)
```

| 属性 | 规格 |
|------|------|
| AI 气泡 | 背景 `#e0f2f1`（Teal 浅色版），左对齐，圆角 12px |
| 用户气泡 | 白色背景 `#ffffff` + 浅灰边框，右对齐，圆角 12px |
| 内容类型 | 纯文本 / KaTeX 化学式 / 工具调用卡片 / 审批卡片 |
| 工具调用卡片 | 嵌入在对话流中，带实时计时器显示 |
| 审批卡片 | 黄色边框 + 米黄背景，含确认/取消两个按钮 |

#### CHATINPUT — 对话输入区

```
出现页面: 1.1(教师端), 2.1(学生端), 3.2(家长浮动AI)
```

| 属性 | 规格 |
|------|------|
| 教师端 | 页面底部固定，多行文本输入框 + 文件上传按钮 + 发送按钮 |
| 学生端 | 底部固定 70px，附件图标 + 单行输入框 + 圆形发送按钮 |
| 家长端 | 浮动面板内，输入框 + 发送按钮 |
| 状态 | 默认(placeholder) / 输入中(文字计数) / 发送中(禁用+灰色) |

#### CHIPS — 快捷提示芯片

```
出现页面: 1.1(6个预设), 2.1(5个预设), 3.2(5个预设)
```

| 属性 | 规格 |
|------|------|
| 外观 | 圆角 20px（胶囊形），浅灰背景，`#1a1a2e` 文字 |
| 排列 | 横向，超出可滚动（学生端/家长端） |
| 交互 | 点击后填充到输入框并发送 |
| 显隐 | 发送消息后隐藏，对话恢复空闲后重新显示 |

---

### 2.3 数据展示组件（出现在 3+ 页面）

#### CARD — 通用卡片

```
出现页面: 全 14 页均有卡片变体
```

| 变体 | 视觉特征 | 使用场景 | 出现页面 |
|------|---------|---------|---------|
| `card-default` | 白色背景 `#ffffff`，浅灰边框，hover 轻微阴影 | 信息列表、数据展示 | 1.2, 1.4, 2.2 |
| `card-emphasis` | 顶部 4px `#d4a574` 暖色装饰条 | 需要突出的卡片（当前进行中考试等） | 1.2(Tab4考试卡片), 1.3(KPI) |
| `card-labnote` | Warm Paper `#faf8f5` 底色 + 左侧 1px 红色边距线 `#b43c28` | 出题工作台、错题本等"做实验"氛围区域 | 1.2(Tab1), 2.3 |
| `card-student` | 含头像+姓名+班级+状态标签的水平布局卡片 | 学生管理列表 | 1.4, 1.5(学生行) |
| `card-exam` | 含标题+状态标签+操作菜单的垂直布局卡片 | 考试/练习任务列表 | 1.2(Tab4), 2.2(任务卡片) |
| `card-question` | 含题目内容+审核徽章+RAG来源+操作按钮的题目卡片 | 出题工作台题目列表 | 1.2(Tab1) |

#### STATCARD — 统计 KPI 卡片

```
出现页面: 1.3, 1.4, 2.3, 2.4, 2.5, 3.2
```

| 属性 | 规格 |
|------|------|
| 结构 | 标签（上，`#6b7280` 小号）+ 数值（下，Oxford Blue 大号 Cormorant Garamond） |
| 加载态 | 数值闪烁骨架屏（灰色脉冲占位条） |
| 空态 | 数值显示 `--` |

#### BADGE — 状态徽章/标签

```
出现页面: 1.2(审核徽章), 1.5(置信度标签), 1.6(状态徽章), 1.7(批改状态), 2.2(知识点chip), 2.3(障碍类型标签), 3.2(学习特点标签)
```

| 变体 | 视觉 | 使用场景 |
|------|------|---------|
| `badge-pass` | 浅绿背景 + `#2c6e49` 深绿文字 | 审核通过、答案正确 |
| `badge-warn` | 浅黄背景 + 深棕文字 | 审核警告、需复核 |
| `badge-block` | 浅红背景 + `#b43c28` 深红文字 | 审核阻断、安全问题 |
| `badge-difficulty` | 简单=青/中等=蓝/困难=红 (绿→蓝→红递进) | 题目难度标签 |
| `badge-obstacle` | 概念=紫/审题=蓝/表述=青 (平行区分，无等级) | 障碍类型标签 |
| `badge-status` | done=绿/processing=黄/failed=红 | 操作状态徽章 (OCR/批改) |

#### AUDITMARK — 四维审核标记

```
出现页面: 1.2(题目卡片)
```

| 属性 | 规格 |
|------|------|
| 维度 | 化学正确性 / 教学适切性 / 认知合理性 / 安全合规性 |
| 通过 | 绿色底 `#2c6e49`，四维均显示 ✓ |
| 警告 | 黄色底，对应维度显示 ⚠，题目可用但建议复核 |
| 阻断 | 红色底 `#b43c28`，对应维度显示 ✗，该题目不会出现在学生端 |
| 阻断行为 | 阻断题目卡片降低不透明度 + 不可选，前端过滤不出现在学生端 |

#### PROGRESS — 进度条

```
出现页面: 1.5(置信度), 1.6(OCR进度), 2.2(练习进度), 2.4(复习进度)
```

| 属性 | 规格 |
|------|------|
| 高度 | 6px |
| 底色 | 浅灰 `#e5e7eb` |
| 填充色 | Oxford Blue `#002147` |
| 动画 | 填充过渡 0.3s ease（OCR 进度使用轮询更新） |
| 变体 | 置信度进度条：≥0.8 高置信(蓝) / 0.7-0.8 中置信(黄，标注"需关注") / <0.7 低置信(红，标注"建议复核") |

#### ACCORDION — 手风琴列表

```
出现页面: 1.5(障碍分组学生列表), 2.3(错题列表)
```

| 属性 | 规格 |
|------|------|
| 模式 | 纯手风琴：同时只展开一个项目 |
| 折叠态 | 标题 + 摘要标签 |
| 展开态 | 完整内容（含操作按钮） |
| 动画 | 展开/折叠 0.2s ease |

#### CHART — 图表组件

```
出现页面: 1.3(3图表), 1.5(障碍分布图), 2.5(知识点掌握度)
```

| 变体 | 类型 | 出现页面 |
|------|------|---------|
| `chart-bar` | 柱状图（知识点错误率） | 1.3 |
| `chart-donut` | 环形图（障碍类型占比） | 1.5 |
| `chart-line` | 折线图（成绩趋势） | 1.3 |
| `chart-bar-h` | 水平条形图（知识点掌握度） | 2.5 |
| 空态 | 图表渲染失败 → "图表加载失败，点击重试" | — |
| 演示模式 | API 不可用时显示"模拟数据"标识，使用内置静态示例 | 1.3 |

---

### 2.4 表单与输入组件（出现在 2+ 页面）

#### INPUT — 输入框

```
出现页面: 1.1(对话输入), 1.2(知识点搜索), 1.4(搜索框), 2.6(登录), 3.1(登录)
```

| 变体 | 视觉 | 使用场景 |
|------|------|---------|
| `input-standard` | 浅灰边框，圆角 8px，聚焦时边框变蓝 + 淡蓝光晕 | 表单、搜索 |
| `input-labnote` | 无边框，仅底部 2px 灰色实线，透明背景；聚焦时底部线变红 `#b43c28` | 错题本、实验记录等"手写感"场景 |
| `input-search` | 标准输入框 + 左侧搜索图标 | 搜索筛选 |

#### SELECTOR — 选择器

```
出现页面: 1.2(难度/题库), 1.3(班级/时间), 1.4(筛选chip/班级), 1.5(班级/考试), 1.6(班级), 3.2(子女选择)
```

| 变体 | 交互 | 出现页面 |
|------|------|---------|
| `select-dropdown` | 下拉单选 | 难度、班级、考试、时间范围 |
| `select-chip` | 多选 chip 按钮组（选中=Oxford Blue 填充） | 题型、知识点、筛选条件 |
| `select-searchable` | 可搜索多选 chip + 输入框 | 知识点搜索 |
| `select-switcher` | 左右箭头 + 名称 + "添加"链接 | 多子女切换 |

#### UPLOAD — 文件上传区域

```
出现页面: 1.1(文件上传按钮), 1.6(拖拽上传区), 1.7(上传区域), 2.1(附件拍照), 1.4(批量导入)
```

| 变体 | 视觉 | 出现页面 |
|------|------|---------|
| `upload-button` | 图标按钮，与输入框同行 | 1.1(对话附件), 2.1(拍照) |
| `upload-dropzone` | 500×300px 虚线框，云上传图标 + 提示文字 + 按钮 | 1.6, 1.7 |
| `upload-inline` | 文件选择按钮 + 预览表格 | 1.4(批量导入) |
| 拖拽悬停 | 虚线框边框高亮为 Oxford Blue | — |
| 约束 | 单文件 ≤10MB，单批 ≤10 张，格式 JPG/PNG/BMP/WEBP/PDF | — |

---

### 2.5 反馈组件（出现在 3+ 页面）

#### TOAST — 通知提示

```
出现页面: 全 14 页通用
```

| 属性 | 规格 |
|------|------|
| 位置 | 右上角浮出 |
| 底色 | Oxford Blue `#002147` + 白色文字 |
| 行为 | 3 秒后自动消失，不阻断用户操作 |
| 场景 | 操作成功/失败反馈、生成完成通知、审批结果通知 |

#### MODAL — 弹窗

```
出现页面: 1.4(添加学生/调班), 2.5(学习报告/学习计划), 3.2(AI解读)
```

| 属性 | 规格 |
|------|------|
| 结构 | 半透明遮罩层（阻断背景操作）→ 白色卡片（圆角 12px）→ 底部按钮区（确认/取消，右对齐） |
| 大内容弹窗 | 可滚动内容区（如学情报告预览） |
| 底部滑出弹窗 | 学习周报、学习计划使用从底部滑出的半屏弹窗 |

#### DRAWER — 侧滑抽屉

```
出现页面: 1.4(学生详情抽屉, 480px), 2.1(侧边栏抽屉, 280px)
```

| 属性 | 规格 |
|------|------|
| 桌面端 | 右侧滑入，480px 宽，含遮罩层 |
| 移动端 | 左侧滑入，280px 宽，含遮罩层 |
| 关闭 | 点击遮罩层关闭 + 滑入动画反转 |

#### SKELETON — 骨架屏

```
出现页面: 1.1(侧边栏), 1.2(题库列表/题目网格), 1.3(KPI卡片), 1.4(学生卡片), 2.2(任务卡片), 2.3(错题列表)
```

| 属性 | 规格 |
|------|------|
| 外观 | 灰色 `#e5e7eb` 占位条，脉冲动画 |
| 变体 | 文本行占位 / 卡片占位 / 数字占位 / 图表区占位 |

#### EMPTYSTATE — 空状态

```
出现页面: 1.1(无历史对话), 1.2(题库为空), 1.3(无考试数据), 1.4(搜索无结果), 2.2(无练习), 2.3(无错题), 2.4(无待复习)
```

| 属性 | 规格 |
|------|------|
| 结构 | 半透明图标（占位符图标或烧杯图标）+ 引导文字（`#6b7280`） |
| 规则 | 不显示"暂无数据"，而是告诉用户"可以做什么" |
| 示例 | "请先创建一次考试"、"暂无错题，继续保持！"、"点击上方开始出题" |

#### ERRORSTATE — 错误状态

```
出现页面: 全 14 页通用
```

| 属性 | 规格 |
|------|------|
| 结构 | 红色错误描述文字 + 重试按钮（Oxford Blue 次按钮样式） |
| 规则 | 必须同时展示：(1) 发生了什么 + (2) 下一步行动 |
| 示例 | "连接中断，点击重试"、"图表加载失败，点击重试" |

#### LOADINGSPINNER — 加载指示器

```
出现页面: 全 14 页通用
```

| 属性 | 规格 |
|------|------|
| 按钮内加载 | 按钮文字替换为旋转图标 + "处理中..." |
| 区域加载 | 三点依次弹跳动画（表示系统在处理，非卡死） |
| 图表加载 | 图表区居中显示加载指示器 |

---

### 2.6 导航与操作组件

#### BREADCRUMB — 上下文标签

```
出现页面: 1.1(对话区顶部上下文标签), 1.2(工作台导航)
```

| 属性 | 规格 |
|------|------|
| 外观 | 胶囊形标签，显示当前选中项（班级/考试/学生） |
| 交互 | 点击可切换或清除 |
| 无选中时 | 整个区域隐藏 |

#### PAGINATION — 分页控件

```
出现页面: 1.4(学生列表)
```

| 属性 | 规格 |
|------|------|
| 布局 | 居中水平排列：首页 / ... / 中间页 / ... / 末页 |
| 当前页 | Oxford Blue 填充，白色文字 |
| 其他页 | 浅灰背景，`#1a1a2e` 文字 |
| 每页数量 | 20 条 |

#### AGENTSTATUS — Agent 状态栏

```
出现页面: 1.1(教师端对话)
```

| 属性 | 规格 |
|------|------|
| 位置 | 输入框上方 |
| 阶段 | 分析中(浅蓝背景) → 执行中(浅蓝+计时器) → 等待审批(浅黄) → 错误(浅红) |
| 内容 | Agent 当前阶段文字 + 工具执行计时器 |

---

### 2.7 特殊组件（单页但跨角色复用）

#### LOGINFORM — 登录表单

```
出现页面: 2.6(学生登录), 3.1(家长登录)
```

| 属性 | 共同特征 | 差异 |
|------|---------|------|
| Logo 区 | "ChemAI 智辅化学" + 副标题 | 家长端增加 Teal "家长端"标签 |
| 输入框 | 圆角 8px，标准输入框样式 | 学生=学号+密码；家长=手机号+绑定码(6位数字) |
| 按钮 | Oxford Blue 全宽，48px 高 | — |
| 加载态 | "登录中..." + 禁用 | — |
| 错误态 | 输入框红色边框 + 抖动动画 + 红色提示文字 | — |

#### FLOATINGBUTTON — 浮动操作按钮

```
出现页面: 3.2(家长端浮动AI按钮)
```

| 属性 | 规格 |
|------|------|
| 位置 | 右下角固定，56px 圆形 |
| 背景 | Oxford Blue `#002147` |
| 图标 | 白色对话气泡图标 |
| 交互 | 点击弹出 AI 对话迷你面板（半透明覆盖 + 消息区 + 输入框 + 5 个预设提示词） |

---

## 第三段：设计理念 → AI 可执行约束规则

> 以下规则从 36 号设计系统的"Academic Catalyst"理念翻译而来。每一条都是 AI 编码时必须检查的硬约束（可自动化 lint），而非模糊的"注意设计感"。

### 3.1 色彩使用规则

| # | 规则 | 违规示例 |
|---|------|---------|
| C1 | **Oxford Blue `#002147` 只用于"系统的声音"**：导航、主按钮、标题、侧边栏。不可用于 AI 对话气泡或用户生成内容 | ❌ 用 Oxford Blue 做 AI 对话气泡背景 |
| C2 | **Teal `#0d7377` 只用于"AI 的声音"**：AI 对话气泡、AI 按钮、化学元素强调。不可用于系统导航或主按钮 | ❌ 用 Teal 做主侧边栏背景 |
| C3 | **Warm Paper `#faf8f5` 只用于"纸的声音"**：页面主背景、阅读区、卡片底色。不可用于强调或操作区 | ❌ 用 Warm Paper 做主按钮颜色 |
| C4 | **状态色使用"浅色背景+深色文字"标签式**，禁止纯色填充按钮 | ❌ 通过=纯绿填充按钮 |
| C5 | **装饰色（氢光谱四色）仅用于背景渐变、装饰分割线、加载动画**，不可用于传达信息或状态 | ❌ 用 Hα 红表示错误状态（应使用 semantic.block_text） |
| C6 | **信息密度控制**：教师端允许高密度（表格、多列），学生端/家长端必须降低密度（卡片化、大触控区） | ❌ 学生端使用教师端的数据表格布局 |
| C7 | **颜色语义一致性**：绿色=正确/通过/安全，黄色=警告/需关注，红色=错误/阻断/危险 — 全局不可反转 | ❌ 用绿色表示"困难"难度 |

### 3.2 实验室笔记本主题规则

| # | 规则 | 适用页面 |
|---|------|---------|
| L1 | **网格纸背景**：24px 网格线，4% 不透明度 `rgba(0,33,71,0.04)`，仅用于内容区底层。如果用户能注意到它 → 透明度太高 | 出题工作台、错题本 |
| L2 | **红色边距线**：卡片左侧 1px 垂直红线 `#b43c28`，仅用于 labnote 卡片变体 | 出题工作台、错题本 |
| L3 | **元素符号导航**：侧边栏导航图标使用元素符号（H/O/C/Fe）+ 衬线字体加粗，不得使用通用图标（如 🏠/⚙️） | 教师端侧边栏 |
| L4 | **化学式水印**：页面背景极淡化学式（3% 不透明度），微倾斜 2 度。水印内容为可逆反应方程式 | 全局背景层 |
| L5 | **主题氛围不干扰可读性**：所有 Lab Notebook 装饰元素的透明度总和不得超过 10%，正文区对比度不低于 4.5:1 | 全局 |

### 3.3 角色-设备-设计语言矩阵

| # | 规则 |
|---|------|
| R1 | **教师端桌面（≥1024px）**：三栏布局。侧边栏深色系（`#f5f0e8`），视觉重心在"效率"。数据密集、表格为主、专业工具感 |
| R2 | **教师端平板（768-1023px）**：两栏布局。侧边栏折叠为仅图标（48px），AI 对话面板隐藏通过按钮呼出 |
| R3 | **学生端/家长端（<768px）**：单栏布局。顶部标题栏 + 内容区 + 底部 TabBar。卡片式、大触控区（最小 44px 触摸目标）、引导式交互 |
| R4 | **学生端与教师端使用不同设计语言**：教师=效率工具；学生=内容理解。组件样式可能同名但视觉权重不同（如教师端 CARD 更紧凑，学生端 CARD 更大更圆润） |
| R5 | **教师端桌面可切换至 430px 视口**预览学生端效果，无需真实移动设备 |

### 3.4 交互行为规则

| # | 规则 |
|---|------|
| I1 | **按钮三态反馈**：悬停 = 颜色加深 + 轻微上浮阴影（`box-shadow` 增加 4px blur）；点击 = 缩小至 97%（`transform: scale(0.97)` + 0.1s transition）；禁用 = 降低不透明度至 50% |
| I2 | **Toast 不阻断操作**：3 秒自动消失，位置固定右上角。与 Modal 严格区分——Modal 阻断操作、需要用户确认 |
| I3 | **手风琴单开**：所有手风琴列表同一时间只展开一个项目，展开另一个时自动折叠前一个 |
| I4 | **数据持久化**：切换 Tab/题目/页面时不丢失用户已输入/已选择的数据（通过客户端 sessionStorage 缓存） |
| I5 | **乐观更新**：标记"已掌握"等操作先更新 UI（卡片消失/变半透明），后台异步同步 API，失败时回滚 + Toast 提示 |
| I6 | **输入框字符计数**：当输入框有最大长度限制时，右下角显示"当前/最大"计数（如绑定码输入框） |
| I7 | **SSE 连接管理**：连接断开 → 显示重试横幅；重连成功后 → 横幅消失；连续 3 次失败 → 建议刷新页面 |

### 3.5 化学内容渲染规则

| # | 规则 |
|---|------|
| F1 | **所有化学式必须使用 KaTeX + mhchem 渲染**，禁止用纯文本 + HTML sub/sup 标签拼凑化学式 |
| F2 | **等宽字体强制**：化学式、化学方程式、数据表格中的数值，必须使用 `JetBrains Mono` 等宽字体 |
| F3 | **后端已格式化**：API JSON 中的化学式已在后端转换为 KaTeX 语法或带上下标格式，前端只需渲染，不需要再做文本→化学式转换 |
| F4 | **备用渲染**：KaTeX CDN 加载失败时，回退到纯文本显示（保留 `\ce{...}` 原始语法作为 fallback 文本） |

### 3.6 Tailwind CSS 使用规则

| # | 规则 |
|---|------|
| T1 | **禁止自定义 CSS 文件**：所有样式通过 Tailwind CDN 类名在 HTML 中直接表达。如需自定义色值，使用 `bg-[#002147]` 方括号语法 |
| T2 | **色值引用必须匹配 Token**：使用 `bg-[#002147]` 而非 `bg-blue-900`，确保与设计 Token 精确一致 |
| T3 | **响应式断点**：使用 Tailwind 标准断点 `lg:`(1024px)、`md:`(768px)，添加自定义断点 `tablet: 768px`、`desktop: 1024px` 到 Tailwind CDN 配置 |
| T4 | **间距遵循 4px 步进**：使用 Tailwind spacing scale（`p-2`=8px, `p-4`=16px, `p-6`=24px...），不引入非标准间距 |

---

## 第四段：整体约束与已知缺口

### 4.1 能力边界：可以做什么

✅ AI 编码助手可以独立完成：
- 根据本文档的颜色 Token、字体 Token、间距 Token 生成所有 14 个页面的 HTML + Tailwind 类名
- 实现所有组件的静态变体（默认态、hover 态、空态、错误态）
- 实现页面间 HTML 结构一致性（全局组件的 class 命名保持一致）
- 实现化学式的 KaTeX + mhchem 渲染
- 实现教师端/学生端/家长端三套响应式布局
- 实现 Lab Notebook 主题的视觉装饰（网格纸、红色边距线、化学式水印）
- 实现按钮三态反馈（hover/active/disabled）
- 实现 Toast 提示、Modal 弹窗、Drawer 抽屉的打开/关闭逻辑
- 实现手风琴折叠/展开、Tab 切换、分页跳转
- 实现骨架屏加载态、空状态引导页
- Agent 导航桥接协议的 SSE 事件监听（`navigate`/`populate`/`action`）

### 4.2 能力边界：不能做什么

❌ AI 编码助手不能（或不应）独立完成：
- **真实 API 对接**：所有数据接口需等后端就绪后联调。当前阶段可做：Mock 数据 + 模拟延迟 + 模拟错误
- **图表库选型与绑定**：Chart 组件（1.3 学情面板等）需要选择图表库（Chart.js/ECharts），此后由人工完成图表配置
- **SSE 流式对话实现**：Agent 对话的 SSE 连接、工具调用卡片的实时渲染，需要与后端 Agent 框架联调
- **OCR 真实上传流水线**：答题卡上传→OCR 识别→结果轮询的完整流程需要后端 OCR 服务就绪
- **跨页面数据桥接**：Agent 导航桥接的 sessionStorage 读写协议需要前后端共同约定数据格式后实现
- **Vue 3 多 Tab 切换**：考试工作台页面使用 Vue 3 CDN 管理状态，具体 Vue 实例化逻辑需人工主导
- **无障碍（a11y）完整合规**：本文档未覆盖屏幕阅读器支持、键盘导航、焦点管理、ARIA 标签等
- **性能优化**：本文档未覆盖资源懒加载、代码分割、CDN 回退策略、图片优化等
- **安全性**：本文档未覆盖 XSS 防护、CSP 策略、输入净化、Token 存储等

### 4.3 已知设计缺口

以下设计决策尚未在 36/40 号文档中明确，需要产品设计补充：

| # | 缺口 | 影响范围 | 建议 |
|---|------|---------|------|
| G1 | **图表库未指定** | 1.3(学情面板图表), 1.5(障碍分布图), 2.5(知识点条形图) | 推荐 Chart.js（轻量 CDN）或 ECharts（功能全） |
| G2 | **移动端下拉刷新具体交互** | 学生端/家长端 | 40 号文档提及"下拉刷新"但未描述具体视觉和交互 |
| G3 | **暗色模式支持** | 全局 | 36/40 号文档均未提及暗色模式，假设仅支持亮色模式 |
| G4 | **教师端批改结果的逐题对比视图** | 1.7(答题卡批改) | 40 号 1.7 节描述较少，批改结果详情页布局待补充 |
| G5 | **家长端 AI 解读"通俗语言转换"的 UI 呈现** | 3.2(学习报告 Tab) | 仅提到"点击展开通俗语言解读"，未描述展开后的对比布局 |
| G6 | **复习中心艾宾浩斯算法升降级的 UI 交互细节** | 2.4(复习中心) | 40 号标注"初期复用错题列表数据源"，完整交互待后续迭代 |
| G7 | **错误边界（Error Boundary）的全局策略** | 全局 | 仅描述了各页面独立错误态，缺少全局 JS 异常捕获和降级 UI |
| G8 | **打印样式** | 教师端（试卷/报告导出） | 考试列表有"导出"操作，但未定义打印/导出 PDF 的样式 |
| G9 | **国际化（i18n）** | 全局 | 当前仅中文，未预留多语言扩展机制 |
| G10 | **动画/过渡时长规范** | 全局 | 按钮/卡片/弹窗的过渡动画时长在文档中分散提及（0.1s/0.2s/0.3s），缺少统一的动画时长 Token 表 |

### 4.4 页面间数据传递协议（已定义，待实现）

40 号文档 4.2 节定义了 4 个跨页面数据传递场景。实现时需遵循以下协议：

```
发送方: sessionStorage.setItem('chemai_bridge', JSON.stringify({
  action: 'populate',    // navigate | populate | action
  target: 'student-detail',
  payload: { studentId: '...' }
}))
接收方: 页面 onload 时读取 chemai_bridge → 解析 → 执行对应操作 → 清除该 key
```

### 4.5 文件组织约定

基于 Vanilla HTML 多页面 + Tailwind CDN 的技术栈，建议的文件组织：

```
frontend/
├── index.html                    # 教师端 → AI Agent 对话主面板 (1.1)
├── teacher/
│   ├── exam-workspace.html       # 考试工作台 (1.2)
│   ├── class-dashboard.html      # 班级学情面板 (1.3)
│   ├── student-management.html   # 学生管理 (1.4)
│   ├── diagnosis.html            # 障碍诊断 (1.5)
│   ├── ocr-upload.html           # 答题卡 OCR 上传 (1.6)
│   └── grading.html              # 答题卡批改 (1.7)
├── student/
│   ├── assistant.html            # AI 助教对话 (2.1)
│   ├── practice.html             # 练习做题 (2.2)
│   ├── mistakes.html             # 错题本 (2.3)
│   ├── review.html               # 复习中心 (2.4)
│   ├── profile.html              # 个人报告与设置 (2.5)
│   └── login.html                # 学生登录 (2.6)
├── parent/
│   ├── login.html                # 家长登录 (3.1)
│   └── dashboard.html            # 家长主面板 (3.2)
├── shared/
│   ├── components.js             # 全局可复用组件 JS（Toast/Modal/Drawer/Skeleton...）
│   ├── chem-bridge.js            # Agent 导航桥接协议
│   └── mock-data.js              # 开发阶段 Mock 数据
└── README.md
```

---

*本文档为 ChemAI 前端开发的工程设计规格。页面设计细节请回溯 [40-前端页面设计规格](./40-前端页面设计规格.md)，视觉系统理念请回溯 [36-设计系统](./36-设计系统.md)。*
