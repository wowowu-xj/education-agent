# Tasks

## 1. 后端预览校验（TDD）

- [x] 1.1 编写批量导入预览失败测试（RED）：断言解析 + 逐题校验、错误明细、不落库
- [x] 1.2 实现 `POST /api/questions/batch/preview`（解析 + 校验，不落库），使 1.1 通过（GREEN）

## 2. 后端确认写入（TDD）

- [x] 2.1 编写批量导入确认失败测试（RED）：断言通过项落库 + 建向量索引、部分成功不整体回滚、返回成功/失败统计与逐题原因、归属当前教师
- [x] 2.2 实现 `POST /api/questions/batch/commit`（重校验 → 逐题写入 + 索引 → 统计），使 2.1 通过（GREEN）

## 3. Tab 2 前端三步流程

- [x] 3.1 「批量导入」入口与预览步骤：解析模板内容 → 调 preview → 展示可导入题数与校验错误明细
- [x] 3.2 确认步骤：预览通过后点击确认 → 调 commit
- [x] 3.3 结果统计步骤：展示成功/失败数及逐题原因，提供「仅看失败」过滤

## 4. 验证

- [x] 4.1 后端测试全绿（`pytest`），`openspec validate question-batch-import --strict` 通过
- [ ] 4.2 浏览器手动验证批量导入预览 → 确认 → 结果统计全流程（含部分失败、全失败空态）
